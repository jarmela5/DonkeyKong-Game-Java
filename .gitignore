# --- Compilação e Binários ---
bin/
out/
*.class
*.jar

# --- Ficheiros de IDEs (Eclipse, IntelliJ, VS Code) ---
.idea/
.settings/
.project
.classpath
*.iml
.DS_Store
.vscode/

# --- Ficheiros de Estado do Jogo (Opcional) ---
# Se queres que o ranking seja partilhado com todos, comenta a linha abaixo.
# Se queres que cada pessoa tenha o seu próprio ranking, mantém.
top10_scores.txt